<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:gravity="center"
    android:padding="5dp"
    android:background="@color/colorSecondary"
    android:id="@+id/rich_link_card"
    android:layout_margin="5dp">

    from https://github.com/ponnamkarthik/RichLinkPreview/blob/master/richlinkpreview/src/main/res/layout/link_layout.xml

    <ImageView
        android:scaleType="centerCrop"
        android:id="@+id/rich_link_image"
        android:layout_width="110dp"
        android:layout_height="90dp" />

    <LinearLayout
        android:padding="2dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <TextView
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            android:id="@+id/rich_link_title"
            android:textColor="@color/colorFontPrimary"
            android:padding="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Data"/>

        <TextView
            android:maxLines="2"
            android:ellipsize="end"
            android:id="@+id/rich_link_desp"
            android:textColor="@color/colorFontDark"
            android:padding="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Data"/>

        <TextView
            android:maxLines="1"
            android:ellipsize="end"
            android:id="@+id/rich_link_url"
            android:textColor="@color/colorFontLink"
            android:padding="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Data"/>

    </LinearLayout>
</LinearLayout>